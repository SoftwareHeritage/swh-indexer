// -*- js -*-

// connect to db
use content

// create single index on column sha1
// unique: key must be unique
// sparse: only return result for which the index exists
// background: build asynchronously
db.content.createIndex({ 'sha1': 1 },
                       { 'unique': true, 'sparse': true, 'background': true})
